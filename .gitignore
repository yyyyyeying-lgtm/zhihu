# ---------- macOS 系统文件 ----------
.DS_Store
._*
.Spotlight-V100
.Trashes
.fseventsd

# ---------- 编辑器与 IDE ----------
.vscode/
.idea/
*.swp
*.swo
*~

# ---------- AI 助手的工作目录 ----------
# 这是本地工具的内部目录（会话记忆、缓存），不属于项目本身
.workbuddy/

# ---------- 本地验证产物 ----------
# tools/verify.sh 会把截图与 DOM 快照写到系统临时目录，不会落到仓库里。
# 如果你手动改成了输出到仓库，这两个规则可以兜底：
/zhiliao-verify/
*.log

# ---------- 凭据（本项目不需要任何后端，出现即说明误提交）----------
.env
.env.*
*.key
*.pem
