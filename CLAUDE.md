# CLAUDE.md · 知了诊所项目工作指引

> 这是本项目的**唯一入口**。开工前先读这里，再按索引去读对应的标准文件。
> 最后更新：2026-09-12 ｜ 当前版本：v1.9

---

## 一、这个项目是什么

**知了诊所**——把一个人的困惑当作一例病例，由 AI 组织一次真实的会诊，
产出一份公开的《会诊结论书》和一份私密的《行动清单》。

- 参赛方向：知乎 AI 黑客松 2026 · 校园新锐季 · **知识炼金场**（单选赛道）
- 当前形态：**可运行的本地网页应用**（纯静态 HTML/CSS/JS），v1.0 全流程已跑通
- 体验地址：本机双击 `知了诊所.app`，或直接打开 `index.html`

---

## 二、目录地图

| 路径 | 是什么 | 能不能改 |
|---|---|---|
| `index.html` `css/` `js/` `assets/` | **应用本体，交付物**（在仓库根目录） | 改代码就改这里 |
| `assets/` | **狐看山医生的形象素材**（头像 / 全身插画） | 换形象只改这里，规则见 `docs/03` §7.4 |
| `js/zhihu.js` | **知乎数据开放平台接入** | 改鉴权头或解析逻辑前看 `docs/02` §五点五 |
| `提交材料/` | **黑客松提交物**：PPT、说明书、路演稿、演示视频、访谈工具包 | 交出去的东西，改前先看 `docs/04` 的 v2.0 |
| `知了诊所-产品方案-终版.docx` | 产品方案，**所有需求的来源** | 只读。要改需求先走变更流程 |
| `docs/` | 全部标准文件 | 随项目演进同步更新 |
| `.workbuddy/memory/` | 会话记忆，自动维护 | 不要手工整理 |

---

## 三、标准文件索引

| 文件 | 管什么 | 什么时候**必须**读 |
|---|---|---|
| `docs/00-开发日志.md` | 追加式工作日志 | 开工前读最近 3 条；**收工前必须追加一条** |
| `docs/01-需求规范.md` | 做什么、不做什么、验收标准 | 动手写任何功能之前 |
| `docs/02-技术规范.md` | 技术栈、目录、命名、数据契约 | 写代码之前 |
| `docs/03-设计规范.md` | 配色、字号、组件、交互原则、文案口吻 | 改任何界面或文案之前 |
| `docs/04-开发步骤.md` | 里程碑、迭代范围、Definition of Done | 决定"下一步做什么"时 |
| `docs/05-测试与验收.md` | 三层验证流程、回归清单、已知限制 | 每次声称"做完了"之前 |

---

## 四、工作说明（铁律）

### 4.1 开工三件事

1. 读 `docs/00-开发日志.md` 最近 3 条 → 知道上次做到哪、留下什么坑
2. 读 `docs/04-开发步骤.md` → 确认**当前迭代的唯一范围**
3. 确认这次只做这一个迭代。**范围外的想法写进日志的「下一步候选」，不要顺手做**

### 4.2 写代码时的约束

- 技术选型已锁定（见 `docs/02-技术规范.md`）。**默认不引入任何依赖、不引入构建工具**
- 命名风格、元素 id、数据契约一律按规范来，不自创
- 动界面之前先过一遍 `docs/03-设计规范.md`，特别是「点击预算」和「术语统一表」

### 4.3 收工四件事（缺一不可）

1. **自测**：按 `docs/05-测试与验收.md` 跑完三层验证
2. **写日志**：往 `docs/00-开发日志.md` 追加一条（用文件顶部的模板）
3. **更新进度**：勾选 `docs/04-开发步骤.md` 里对应的检查项
4. **同步规范**：如果这次改动影响了需求、技术或设计决策，同步更新对应规范文件

### 4.4 三条不能破的线

- ❌ 不为"看起来更快"跳过自测。本项目已经发生过一次「一个 id 写错导致全流程静默中断」
- ❌ 不一次性改动多个模块。一次迭代只碰一个模块
- ❌ 不删除 `docs/`、`tools/` 与仓库根目录下的任何文件

---

## 五、和用户怎么协作

用户是**非技术背景**，请遵守：

- **不要丢命令行让他自己跑**。凡是要执行的操作，替他做好，或给"双击即可"的方案
- **说"做好了"之前，先自己确认真的能跑**。截图或断言结果一起给出来
- **结论先行、少用术语、给选项不给难题**。三选一永远比开放式提问好
- 交付物放在项目根目录，用 present_files 呈现，不要让用户自己找文件

---

## 六、当前状态速览

| 项 | 状态 |
|---|---|
| 应用版本 | **v1.9** |
| 三条分支 | ① 存在真实分歧 → 完整会诊（含会诊手记）｜② 已确诊 → 直接给共识结论｜③ 信息不足 → 给提问草稿、交还社区 ✅ |
| 主流程 | 挂号 → 问诊 → 病灶确认 → 分歧地图 → **按判定分叉** → 行动清单 → 归档 ✅ |
| 现场演示 | `P` 自动演示 · `空格` 暂停/跳过 · `R` 一键重置 · `Esc` 关浮层（全单键，不依赖插件）✅ |
| 人物形象 | **狐看山医生**，形象与称谓全项目唯一（素材在 `assets/`，规则见 `docs/03` §7.4）✅ |
| 功能闭环 | 行动清单可勾选进度（写回档案）· 回访日期 · 档案进度条 ✅ |
| 真实 AI | 判定真的驱动分支 · 请求带超时 · **降级在界面上可见** · 每张推理卡标注来源 ✅ |
| 产出闭环 | 结论书 / 已确诊结论 / 行动清单一键复制为 Markdown（清单带勾选状态）✅ |
| 知乎数据源 | 已接入官方开放平台站内搜索：**分歧地图检索真实站内讨论**，未配置时回退并标注 ✅ |
| 演示入口 | 首屏有「直接看一次完整演示」按钮；`?demo=1` 可打开即播 ✅ |
| 手机适配 | 375px 下无横向溢出（含会诊室）；桌面端像素级零变化 ✅ |
| 提交材料 | 路演 PPT · 说明书 · 路演稿 · 演示视频 · **用户访谈工具包** → `提交材料/` |
| 自动化验证 | **81 项** DOM 断言全绿（11 组，含 375px 手机溢出检测），21 个截图阶段 ✅ 一条命令复跑：`bash tools/verify.sh` |
| 已知限制 | 会诊参与者身份为模拟；分歧地图用离线语料；健康/理财类目未开放（详见 `docs/05` 第五节） |
| 下一步 | **补用户访谈**（`提交材料/知了诊所-用户访谈工具包.md` —— 唯一短板，只有用户本人能做） |

**试三条分支**：打开应用，分别点首页那三个示例困惑，就能看到同一套流程如何走出三种结果——
第一句走完整会诊，第二句直接给结论（不请人），第三句连结论都不给、只给你一个更好的问题。

**要给它做演示**：按 `P`，它会自己走完整个流程，你专心讲；按 `空格` 可以随时停下来回答问题；
讲完按 `R` 一键回到开头，再来一遍。想更干净就先去设置里打开「演示模式」。

**要试真实 AI**：设置里切到「接入真实 AI」，填任一家的 Key。注意界面上每张推理卡
左上角会标出数据来源 ——「真实 AI 推理」是真的接通了，「内置示例数据」说明这步降级了。
**降级一定会写明原因，不会偷偷换成示例数据。**

### 怎么跑项目（给下一个人）

```bash
bash tools/verify.sh        # 一条命令：静态检查 + 全流程驱动截图 + DOM 断言
```

要人工体验，双击根目录下的 `知了诊所.app` 即可。
